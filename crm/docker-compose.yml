# Deprecated: use the root compose file instead.
#   cd .. && docker compose up --build
#
# This stub only forwards to the root file. The explicit name keeps both
# entrypoints on the same Compose project so ports/volumes aren't duplicated.
name: potato-merch-ai

include:
  - path: ../docker-compose.yml
